# Shroud of Turin — Residual Light Event

**Zelenka, D. D. (2026)**
Zenodo. https://doi.org/10.5281/zenodo.19076272

---

### `light_event_shroud_2026.json`
Machine-readable condensation of the paper. Keys: `meta`, `abstract`, `axioms`, `lemmas`, `evidential_tree`, `main_theorem`, `predictions`, `falsification_conditions`, `objections`, `equations_summary`, `expansion_instructions`. Claim type: `FRAMEWORK` — all mechanism claims are explicitly speculative.

### `shroud_model.py`
Qualitative inverse-square projection model. Three body-like implicit surfaces enclosed in a cylinder (`y² + z² = 2`); each surface point emits `I = I₀ / r²` to the cylinder wall. Output: 3D surface view (left) and unwrapped flux image rendered as photographic negative (right). Demonstrates body-distance tonal encoding and photonegative character from geometry alone — no physical units implied.

Dependencies: `numpy`, `scipy`, `scikit-image`, `matplotlib`
